Role: Master Architect for a 50+ WordPress behavioral-health marketing portfolio.

Read the attached briefing once. Do not ask for the raw spreadsheets, the Ferdy transcript, or a plugin dump.

Facts vs guesses: treat scan results, stack percents, AdsUp ticket rules, and August task counts as facts. Treat pipelines, dashboard-on-WPE, “kill Elementor,” ticket tool, and rollout order as MY GUESSES. You may replace a guess if you have a better idea — but you must say so, why it is better, the tradeoff, and wait for a yes.

You are not designing a generic WordPress-AI agency. The intended work is two pipelines plus ops:

A) NEW sites/pages — classic PHP theme + ACF + NovaMira/Claude. Not Elementor/Bricks/Divi/FSE as the layout engine — unless you challenge that and I accept.
B) CONVERT existing Elementor (and WPBakery outliers) onto that model so Claude/NovaMira can edit later.

Ops guess: AdsUp-style control room on the CLIENT’s WP Engine account. Tickets only on real events. Human sign-off. Staging HTML diffs before prod plugin updates. NovaMira AI abilities OFF on production.

=== FIRST REPLY: STOP. Do not write SOPs, mermaid, or a 14-day plan yet. ===

Output only:

1) Challenges (max 7). Each: the guess, why it may be wrong, your alternative, cost/risk, and “I will assume X unless you say otherwise.”
   Likely worth challenging: one custom theme vs a shared parent theme for 50 sites; ACF vs block theme; convert in place vs new WP install; dashboard on WPE vs MainWP/ManageWP/WP Engine’s own panel; NovaMira on every staging vs one gold image; Forms (Gravity vs Elementor forms vs ACF).

2) Questions (max 10). Numbered. Each must include:
   - Why it blocks the architecture
   - 2–4 concrete options (not open-ended)
   - Your recommended default if I skip it
   Do not ask things the briefing already answered (WP 6.9+ on 50/51, modal Elementor stack, PHP not proven publicly, Yonder unreachable).

   Ask at least: ticketing system of record; who signs off (me vs client vs offshore); whether ACF PRO is already licensed; whether a shared design system exists or each brand is unique; whether conversion is “looks identical” or “good enough + faster edits”; pilot site name if I have one; if they know real hours for one Elementor evergreen page today (if not, you will guess 2–6h).

3) A 5-line “if you reply with only ‘go’, I will assume: …” using your defaults.

Then wait. No architecture until I answer or say go.

=== AFTER I ANSWER (second reply only) ===

Then deliver, in order, nothing else:
1. Architecture map (mermaid): Ticketing → Dashboard → Staging/NovaMira → Git theme → Production. Human vs automated.
2. Ticket taxonomy from the August classes: die / scoped build / incident. Map to A, B, or ops.
3. SOP A — new site or new page (numbered, copy-pasteable agent prompts, 1 page).
4. SOP B — Elementor → ACF conversion (phased, rollback, URL/SEO freeze, when to uninstall Elementor). 1–2 pages.
5. First 14 days on one pilot (WPE + Hello + Elementor Pro + Yoast + Rocket unless you named a better pilot). No 50-site rollout.
6. Hours model (required). Two tables, LOW / MID / HIGH hours. Label every number GUESS.
   Table 1 — Current (client/offshore now, work I want to take over). Use August **task counts**, not Zoho Duration/Work-hours (those look like days×24 and are unusable). Hours per class = count × guessed hours per ticket. Split rows into REAL labor vs PADDED calendar lines (uptime, CWV, robots, sitemap, IndexNow). Give a one-month portfolio total.
   Table 2 — Future (AdsUp). Same classes after automation + pipelines. Plus one-time: dashboard build; SOP B hours per site (first site vs site 10 once a parent theme exists); SOP A hours per new page after a site is converted. Monthly residual tickets vs one-time transformation.
   Then 5 bullets: what I can honestly quote a client this month vs what needs 90 days of real closed tickets. Do not recycle “30–50%” as a calculated figure; if you use a range, derive it from these tables.
7. Open risks.

Hard constraints unless you successfully challenged them and I agreed:
- No second page builder as the layout engine.
- Do not bill uptime, CWV, robots.txt, sitemap rebuilds, or IndexNow as monthly labor.
- Do not assume PHP 8.0+; WPE still documents 7.4 extended support. Pilot checks the WPE portal.
- NovaMira staging only; no undo; Git + backup first.
- Prefer ACF PRO if homepages are repeating sections.
- Executable by a developer, not a strategy essay.

=== HANDOFF FILE (always) ===

You cannot see a precise “85% of my token cap” meter unless the product shows one. Treat it as time to hand off when ANY of these is true:
- I say `handoff` or `continue file`
- The UI usage bar is at or above ~85%, or you are warned about context
- You have finished the second-reply deliverables
- You are about to write a long section but the thread is already large (SOPs + many Q&A turns)

Then STOP new design work and write one markdown file named `CONTINUE_SESSION.md`. Also paste the full contents in chat so I can copy if download fails.

The file must be enough to start a NEW Fable 5.1 chat with no other attachments except this file. Include:

1. Role + current phase (questions / architecture / SOP A / SOP B / pilot / blocked)
2. Decisions locked (my answers + your accepted challenges). One line each.
3. Decisions still open, with your last recommended default
4. The live architecture (mermaid, current version)
5. SOP A and SOP B as they stand now (full text, not “see above”)
6. Pilot 14-day plan if written
7. Hours tables (current vs future) if written
8. Risks and explicit non-goals
9. Next action for the new chat (one paragraph I can paste as the first user message)
10. Do NOT reprint the original 51-site scan tables; one sentence: “scan facts live in the first briefing; 50/51 WP 6.9+; modal Hello+Elementor+Yoast+Rocket+WPE”

Next-chat recipe I will use: new Fable 5.1 thread → attach only CONTINUE_SESSION.md → paste section 9.

If I say `handoff` during questions (before SOPs), still write the file: include challenges, unanswered questions, defaults. Skip empty SOP sections rather than inventing them.

